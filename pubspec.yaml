name: firebase_google_auth
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^4.5.6
  
  firebase_auth_repository: ^0.0.1   
 # path: ../firebase_auth_repository
